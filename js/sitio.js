//Iniciamos wow 
new WOW().init();

//inicializa  smooth scroll
//
/*var scroll = new SmoothScroll('a[href*="#"]', {
	
	// Speed & Easing
	speed: 6000, // Integer. How fast to complete the scroll in milliseconds
	offset: 6000, // Integer or Function returning an integer. How far to offset the scrolling anchor location in pixels
	easing: 'easeInOutCubic', // Easing pattern to use
	customEasing: function (time) {}, // Function. Custom easing pattern
});*/

/*$("document").ready(function($){

$(window).width(function () {
    if ($(window).width() <= 480) {
    	$('#seccion').addClass('wow fadeInUp');
	}
 	else {
    	$('#seccion').removeClass('wow fadeInUp');
	}
});
});*/